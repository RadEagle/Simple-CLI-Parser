## How to Run:
1. Clone the repo
2. Ensure you have NodeJS
3. In your bash terminal, run `npm start -- -t [name of task]`

## Dependencies
You will need to install the following libraries through NodeJS:
* Commander
* Zod
* TypeScript

## Options
There are other options than -t, Title. These are:
* -t, --title <title>        The title of the task
* -p, --priority <priority>  The priority of the task
* -d, --dueDate <dueDate>    The due date of the task
* --json                     Output the result in JSON format (default: false)

## Thanks
* roadmap.sh
